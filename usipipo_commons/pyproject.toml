[project]
name = "usipipo-commons"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "mowgliph", email = "jelvysc@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
usipipo-commons = "usipipo_commons:main"

[build-system]
requires = ["uv_build>=0.10.10,<0.11.0"]
build-backend = "uv_build"
